import { NextPage, Metadata } from "next";

import Component from "./Component";

export const metadata: Metadata = {
  title: "次星おみくじ2025",
};

const Page: NextPage = () => {
  return <Component />;
};

export default Page;
